# Scripts

Operational and utility scripts for managing the fullstack-learn monorepo.

- `setup.sh` / `setup.bat`: Developer environment installation helper.
- `seed.sh`: Database seeding utility.
- `validate.sh`: Comprehensive monorepo checks (lint, format, test, build).
